
<div class="row container mx-auto mt-12">
<%# <%= search_form_for @q do |f| %> 

<h1 class="text-3xl font-bold mb-4">
  Stunning royalty-free images & videos
</h1>
<h2 class="text-lg text-gray-600">
Download high quality stock images, videos shared by our community.

<br>
  <%# <% end %> 
  <div class="w-full">
  <% if notice.present? %>
    <p class="inline-block px-3 py-2 mb-5 font-medium text-green-500 rounded-lg bg-green-50" id="notice"><%= notice %></p>
  <% end %>
  <div class="flex items-center justify-between">
    <%#= link_to 'New post', new_post_path, class: "rounded-lg py-3 px-5 bg-blue-600 text-white block font-medium" %>
  </div>
  <div id="posts" class="min-w-full grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4">
    <%= render @posts %>
  </div>
</div>
